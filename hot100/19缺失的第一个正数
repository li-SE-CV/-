用空间换时间-用set集合,对于其它的困难题，这道困难题简单多了代码也容易理解
时间复杂度为O（n）
空间复杂度为O（n）
dict与set的区别（元素唯一，无序）：
dict = 带 value 的 hash 表
set = 只有 key 的 hash 表

代码为：
class Solution:
    def firstMissingPositive(self, nums: List[int]) -> int:
        # nums[:] = [max(0, nums[i]) for i in range(len(nums))]
        s = set(nums)
        min_num = min(nums)
        max_num = max(0,max(nums))
        if min_num > 1:
            return 1
        for i in range(1,max_num):
            if i not in s:
                return i
        return max_num + 1
