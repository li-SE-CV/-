用空间换时间-用set集合
时间复杂度为O（n）
空间复杂度为O（n）
目前已知set的

代码为：
class Solution:
    def firstMissingPositive(self, nums: List[int]) -> int:
        # nums[:] = [max(0, nums[i]) for i in range(len(nums))]
        s = set(nums)
        min_num = min(nums)
        max_num = max(0,max(nums))
        if min_num > 1:
            return 1
        for i in range(1,max_num):
            if i not in s:
                return i
        return max_num + 1
